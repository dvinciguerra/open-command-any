# frozen_string_literal: true

RSpec.describe Open::Command::Any do
  it "has a version number" do
    expect(Open::Command::Any::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
